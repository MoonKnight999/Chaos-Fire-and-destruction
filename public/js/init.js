const game = new Game({
    canvas: document.getElementById('mainScreen'),
    width: innerWidth,
    height: innerHeight
})
const imagePaths = {
    human: "assets/Human.png"
}
const updateMethods = {}